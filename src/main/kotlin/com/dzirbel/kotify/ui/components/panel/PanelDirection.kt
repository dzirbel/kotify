package com.dzirbel.kotify.ui.components.panel

/**
 * Determines the relative position of the panel in a [SidePanel], e.g. [PanelDirection.LEFT] places the panel to the
 * left of the main content.
 */
enum class PanelDirection { LEFT, RIGHT, TOP, BOTTOM }
